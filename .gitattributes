# Set the default behavior for text files
* text=auto

# Explicitly declare files that are binary (non-text) and should not have line endings modified
*.png binary
*.jpg binary
*.gif binary
*.woff2 binary
*.jar binary

# Force LF line endings for specific file types (e.g., JavaScript, CSS, HTML, and more)
*.js text eol=lf
*.css text eol=lf
*.html text eol=lf
*.json text eol=lf
*.md text eol=lf

# Exclude specific directories or files if needed
/excluded-directory/* -text
/excluded-file.txt -text
